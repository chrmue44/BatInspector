<Window x:Class="BatInspector.Forms.frmSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:local="clr-namespace:BatInspector.Forms"
        mc:Ignorable="d"
        Title="frmSettings" Height="450" Width="800">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="35" />
    </Grid.RowDefinitions>
    <WindowsFormsHost Name="_wfHost">
      <wf:PropertyGrid x:Name="_pg"></wf:PropertyGrid>
    </WindowsFormsHost>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
      <Button Name="_btnFactSettings" Width="100" Margin="5" Click="_btnFactSettings_Click">Factory settings</Button>
      <Button Name="_btnCancel" Width="100" Margin="5" Click="_btnCancel_Click">Cancel</Button>
      <Button Name="_btnOk" Width="100" Margin="5" Click="_btnOk_Click">OK</Button>

    </StackPanel>
    </Grid>
</Window>
